html
    head
        title Simple template
    body
        h1 Zaloguj się danymi: l:admin h:admin

        include ./form.pug
            
        if(user)
            h1 Hi, #{user.name}
        else
            a(href="/auth/google") Sign Up